# OpenRouter API key (required for Jev decisions)
OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxx

# Etherscan API key (optional - free tier works without key, but rate-limited)
ETHERSCAN_API_KEY=

# Optional: custom thresholds
# ETH_MIN_VALUE=100
# BTC_MIN_VALUE=50
